package com.kh.array;

public class ArrayListPractice
{
    /*
     * ArrayList : 크기 조정이 가능한 배열 클래스
     * 
     * [선언 방법]
     * (1) import 추가
     *     import java.util.ArrayList;
     *     
     * (2) 선언 및 생성
     *      * ArrayList<타입> 배열이름 = new ArrayList<[타입]>([배열길이]);
     *       ※ [타입],[배열길이] => 생략가능
     *       - 타입 > 자료형 x, String, Class객체, 참조 자료형..
     * (3) ArrayList 기능(메소드)
     *      - 조회 : get(인덱스)
     *              배열이름.get(인덱스); // 배열에서 인덱스 위치의 값을 조회
     *      - 추가: add(값)
     *      - 삭제: remove(인덱스)
     *      - 배열길이 : size()
     *      - 배열에 데이터가 없는지 확인 : isEmpty()
     *      - 값의 인덱스(위치) 찾기: indexOf(값)
     *      
     */
    public static void main(String[] args)
    {
        //사용자에게 이름이랑 휴대폰번호를 입력받는다
        //ArrayList를 활용하여 주소록 프로그램
        
        //추가
        //조회 이름이나 번호로 찾을수 있도록
        //   화면에 출력 => "이름: 번호"
        //(+)삭제
        //(+)추가, 중복된 번호가 있는지 체크
        
        
    }
}
